'use strict'
const IMGS_KEY = 'imgs';

// var img = createImg('img/popo.jpg',['happy']);
// console.log(img);

function createImg(url,keywords) {
    return {
        id: makeId(),
        url,
        keywords,
    }
}

function createImgs(){
    if(!localStorage.)    
}